{
  "name": "elearning",
  "version": "1.0.0",
  "description": "run a command, it spins up the application, than the cli asks questions:\r 1. create a superuser: (login, password)\r 2. Have you created an .env file based on a .env.example file? yes/no\r    -- verifying the .env file...\r 3. happy teaching :)",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^5.3.0",
    "dotenv": "^16.4.5",
    "readline": "^1.3.0",
    "readline-sync": "^1.4.10"
  }
}
